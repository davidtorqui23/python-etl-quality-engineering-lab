# Gherkin Vocabulary Standard

## Datasets

Always use:

Given an orders dataset exists

Avoid:

Given a file exists

Given the dataset exists

Given an input file exists

---

## Loading

Always use:

When the dataset is loaded

Avoid:

When the file is loaded

When the CSV is loaded

When orders are loaded

---

## DataFrames

Always use:

Then a DataFrame should be returned

Avoid:

Then records are returned

Then results are returned

---

## Validation

Always use:

Then validation should fail

Avoid:

Then an error occurs

Then processing stops

---

## Missing Files

Always use:

Given the dataset path does not exist

---

## Empty Files

Always use:

Given the dataset is empty